# Ticket UI

Responsive Android-friendly transit-ticket mockup with a live countdown and QR frame color slider.

## Run locally on Android

```bash
pkg install git python
git clone https://github.com/puranjayyadav/ticket.git
cd ticket
python -m http.server 8000
```

Open `http://localhost:8000` in Chrome.
